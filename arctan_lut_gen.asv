% the function is used to generate the lookup table
% the input is a number to determine the length of the 
function lut = arctan_lut_gen(lim)

lut = zeros(lim+1, 3);

for i = 1:lim+1
    lut(i,2) = i-1;
    lut(i,3) = fi(atan(2^(-i+1)), 1, 32, 30);
end
